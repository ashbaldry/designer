import { CheckboxGroupInput } from '../CheckboxGroup'

test('sanity test - checkbox constructs successfully', () => {
  const component = new CheckboxGroupInput()
  expect(component).toBeDefined()
})
